{
  "name": "trading-setup-scanner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Trading setup scanner prototype based on OI, market cap, funding, price, and volume.",
  "scripts": {
    "check": "node scripts/check-market-data.mjs",
    "collect": "node scripts/collect-snapshot.mjs",
    "collect:loop": "node scripts/collect-loop.mjs",
    "report": "node scripts/generate-report.mjs",
    "report:test": "node scripts/generate-report.mjs --mode=test",
    "scan": "node scripts/scan-snapshots.mjs",
    "scan:test": "node scripts/scan-snapshots.mjs --mode=test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {}
}
